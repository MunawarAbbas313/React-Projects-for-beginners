import React from "react";
//in js file we create the context that will be used in
// every place when ever we will  use the hook useContext
const UserContext = React.createContext();


export default UserContext;